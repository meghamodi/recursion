Given a list, determine if the list is in increasing order.
def is_increasing(lst):
    if len(lst) in [0,1]:
        return True
    if lst[0] < lst[1]:
        return is_increasing(lst[1:])
    return False

print(is_increasing([1,3,6,2]))


Given a list, output a new list that only retains the numbers from the list that is greater than n
def filter_gt(lst,n):
    if n < lst[0]:
        return lst[0:]
    if n > lst[0]:
        return filter_gt(lst[1:],n)

print(filter_gt([1,3,4,6,7], 0))

